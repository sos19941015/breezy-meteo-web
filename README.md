# Open-Meteo Flutter Web

Flutter Web app using Open-Meteo APIs.

## Features
- City search (Open-Meteo geocoding API)
- Current weather
- Next 24 hours forecast
- Next 7 days forecast

## Run
1. Install Flutter SDK and add `flutter` to PATH.
2. In this folder:

```powershell
flutter pub get
flutter run -d chrome
```

## Notes
- This environment did not have Flutter SDK available, so I could not run `flutter pub get` or launch Chrome here.
- App entry: `lib/main.dart`.
